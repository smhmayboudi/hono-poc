export class Error{{pascalCase domain}}Example extends Error {
  constructor(country: string) {
    super(`{{pascalCase domain}} should not be used in ${country}`);
    this.name = "Error{{pascalCase domain}}Example";
  }
}
